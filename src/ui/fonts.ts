import { Protest_Revolution, Roboto_Condensed } from "next/font/google";

// Display fonts
export const robotoCondensed = Roboto_Condensed({ subsets: ["vietnamese"] });

// Logo font
export const protestRevolution = Protest_Revolution({
  weight: ["400"],
  subsets: ["vietnamese"],
});
